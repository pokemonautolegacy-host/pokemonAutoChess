import { GameObjects } from "phaser";
export declare class PokemonSpecialDetail extends GameObjects.DOMElement {
    dom: HTMLDivElement;
    constructor(scene: Phaser.Scene, dialog: string, dialogTitle?: string);
}
